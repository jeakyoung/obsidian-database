---
title: 신진SM 09.15 업무 미팅
date: 2026-09-15
type: 회의록
project: 신진SM
status: 완료
attendees: []
tags: []
---

# 신진SM 09.15 업무 미팅

## 1. 개요

| 항목 | 내용 |
|------|------|
| 일시 | 2026-09-15 |
| 장소 |  |
| 참석자 |  |
| 안건 |  |

## 2. 논의 내용

B/F

실적관리 -> 생산량이 아닌 양품수량으로 카운트하기 ( 포장 )

출하 데이터를 집계시에 실적이없는 수주건에 대해서도 출하데이터가 같이 카운트 되는 현상이 발생함 ->202609S06518 수주번호 조회 

사용자 별 칼럼 고정 기능 -> 테스트 필요 ( 가끔 풀림 )

![[Pasted image 20260915104409.png]]

POP상의 작업지시서 조회시에도 같은수주건이라도 품목 3건이 다나와야됨 - CUST_ITEM_CODE가 누락되어있어 작업지시서 조회시에도 조회가 안되는 중임 
 
요구 사항ㅌ

리포트
-> ERP

시점재고

재고자산 수불리스트 -> MES로 이관 필요

## 3. 결정 사항

## 4. 후속 조치

## 5. 참고



보안 건은 그대로 두겠습니다. 양식을 더 보기 좋게 다듬겠습니다. 먼저 Quartz의 커스텀 CSS 지원 방식을 확인하겠습니다.

Bash

$ cd "$LOCALAPPDATA/Temp" && rm -rf qz && git clone -q --depth 1 --branch v4.4.0 https://github.com/jackyzha0/quartz.git qz 2>/dev/null; cd qz && ls quartz/styles/ && echo "=== custom.scss 참조 위치 ===" && grep -rn "custom.scss" quartz/ --include="*.scss" --include="*.ts" | head

base.scss

callouts.scss

custom.scss

syntax.scss

variables.scss

=== custom.scss 참조 위치 ===

quartz/plugins/emitters/componentResources.ts:8:import styles from "../../styles/custom.scss"

Bash

$ cd "$LOCALAPPDATA/Temp/qz" && cat quartz/styles/custom.scss && echo "=== 지원 callout 종류 ===" && grep -oE '^\s*&\[data-callout="[^"]+"\]' quartz/styles/callouts.scss | grep -oE '"[^"]+"' | tr -d '"' | tr '\n' ' '

@use "./base.scss";

// put your custom CSS here!

=== 지원 callout 종류 ===

abstract info todo todo tip success question warning failure danger bug bug danger example quote

커스텀 CSS 훅이 있습니다. 양식을 다시 설계하고 마이그레이션을 안전하게 처리하겠습니다.

See `_templates/_문서 양식 명세.md`. Existing content is never deleted: its

headings are demoted so they nest under the step that owns legacy content.

Idempotent - already-formatted notes only get missing steps appended.

Idempotent. Also migrates notes written against an earlier revision of the

spec (plain `## 1. 개요` headings) to the current one instead of re-wrapping.

"""

import os, io, re, sys

ROOT = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "Jeakyoung_Blog")

APPLY = "--apply" in sys.argv

# type -> (step2, step3, step4, legacy-landing-step-index, meta rows)

DASH = "—" # em dash, used for "not filled in yet"

SPEC = {

"작업": ("현상 및 원인", "조치 내용", "검증 및 결과", 3,

[("요청자", ""), ("요청일", "@date"), ("대상 시스템", ""), ("관련 화면·프로그램", "")]),

"기술문서": ("배경 및 요구사항", "설계 및 구현", "검증", 3,

[("대상 시스템", ""), ("적용 범위", "@category"), ("관련 모듈", "")]),

"회의록": ("논의 내용", "결정 사항", "후속 조치", 2,

[("일시", "@date"), ("장소", ""), ("참석자", "@attendees"), ("안건", "")]),

"참고자료": ("환경 및 전제", "상세 내용", "주의사항", 3,

[("용도", ""), ("대상 시스템", ""), ("최종 확인일", "@date")]),

"작업": {

"steps": ["📋 개요", "🔍 현상 및 원인", "🔧 조치 내용", "✅ 검증 및 결과", "🔗 참고"],

"plain": ["개요", "현상 및 원인", "조치 내용", "검증 및 결과", "참고"],

"landing": 3,

"meta": [("요청자", ""), ("요청일", "@date"),

("대상 시스템", ""), ("관련 화면·프로그램", "")],

},

"기술문서": {

"steps": ["📋 개요", "🎯 배경 및 요구사항", "🏗 설계 및 구현", "✅ 검증", "🔗 참고"],

"plain": ["개요", "배경 및 요구사항", "설계 및 구현", "검증", "참고"],

"landing": 3,

"meta": [("대상 시스템", ""), ("적용 범위", "@category"), ("관련 모듈", "")],

},

"회의록": {

"steps": ["📋 개요", "💬 논의 내용", "✅ 결정 사항", "🔜 후속 조치", "🔗 참고"],

"plain": ["개요", "논의 내용", "결정 사항", "후속 조치", "참고"],

"landing": 2,

"meta": [("일시", "@date"), ("장소", ""), ("참석자", "@attendees"), ("안건", "")],

},

"참고자료": {

"steps": ["📋 개요", "⚙️ 환경 및 전제", "📖 상세 내용", "⚠️ 주의사항", "🔗 참고"],

"plain": ["개요", "환경 및 전제", "상세 내용", "주의사항", "참고"],

"landing": 3,

"meta": [("용도", ""), ("대상 시스템", ""), ("최종 확인일", "@date")],

},

}

DIRT = {"01_Tasks": "작업", "02_TechDocs": "기술문서", "03_TechDocs": "기술문서",

"03_Meetings": "회의록", "04_References": "참고자료"}

FENCE = re.compile(r"^(\s*)(```|~~~)")

HEADING = re.compile(r"^(#{1,6})(\s+)(.*)$")

return ""

v = m.group(1).strip()

if v in ("[]", ""):

# maybe a block list

items = re.findall(r"^%s:[ \t]*\n((?:[ \t]+-[ \t]*.*\n?)+)" % re.escape(key), fm, re.M)

if items:

return ", ".join(x.strip().lstrip("-").strip() for x in items[0].splitlines() if x.strip())

blk = re.findall(r"^%s:[ \t]*\n((?:[ \t]+-[ \t]*.*\n?)+)" % re.escape(key), fm, re.M)

if blk:

return ", ".join(x.strip().lstrip("-").strip() for x in blk[0].splitlines() if x.strip())

return ""

if v.startswith("[") and v.endswith("]"):

inner = v[1:-1].strip()

return inner

return v[1:-1].strip()

return v.strip('"').strip("'")

def iter_code_mask(lines):

"""True for lines inside a fenced code block."""

def code_mask(lines):

mask, fence = [], None

for ln in lines:

m = FENCE.match(ln)

def strip_leading_h1(body):

"""The generated document H1 replaces any H1 the note opened with."""

lines = body.split("\n")

for i, ln in enumerate(lines):

if not ln.strip():

def shift_headings(body):

"""Demote every heading so the shallowest becomes H3 (nests under an H2 step)."""

lines = body.split("\n")

mask = iter_code_mask(lines)

mask = code_mask(lines)

levels = [len(HEADING.match(l).group(1))

for l, inc in zip(lines, mask) if not inc and HEADING.match(l)]

if not levels:

if inc or not m:

out.append(l)

continue

lvl = min(6, len(m.group(1)) + shift)

out.append("#" * lvl + m.group(2) + m.group(3))

out.append("#" * min(6, len(m.group(1)) + shift) + m.group(2) + m.group(3))

return "\n".join(out)

def meta_table(rows, fm):

out = ["| 항목 | 내용 |", "|------|------|"]

for label, src in rows:

val = fm_get(fm, src[1:]) if src.startswith("@") else src

out.append("| %s | %s |" % (label, val))

return "\n".join(out)

def migrate_headings(body, spec):

"""Rewrite step headings written against older spec revisions."""

for i, (plain, new) in enumerate(zip(spec["plain"], spec["steps"]), start=1):

body = re.sub(r"^##[ \t]+%d\.[ \t]+%s[ \t]*$" % (i, re.escape(plain)),

"## " + new, body, flags=re.M)

body = re.sub(r"^##[ \t]+%s[ \t]*$" % re.escape(plain),

"## " + new, body, flags=re.M)

return body

def steps_for(dtype):

s2, s3, s4, _, _ = SPEC[dtype]

return ["개요", s2, s3, s4, "참고"]

def is_formatted(body, spec):

return all(re.search(r"^##[ \t]+%s[ \t]*$" % re.escape(s), body, re.M)

for s in spec["steps"])

def build(title, dtype, fm, legacy):

s2, s3, s4, landing, rows = SPEC[dtype]

names = steps_for(dtype)

parts = ["# %s" % title, ""]

for i, name in enumerate(names, start=1):

parts.append("## %d. %s" % (i, name))

parts.append("")

if i == 1:

parts.append(meta_table(rows, fm))

parts.append("")

if i == landing and legacy.strip():

parts.append(legacy.strip())

parts.append("")

return "\n".join(parts).rstrip() + "\n"

def meta_table(spec, fm, existing=None):

rows = []

for label, src in spec["meta"]:

val = fm_get(fm, src[1:]) if src.startswith("@") else ""

if existing and existing.get(label):

val = existing[label]

rows.append("| **%s** | %s |" % (label, val if val else DASH))

return "\n".join(["| 항목 | 내용 |", "|:--|:--|"] + rows)

def already_formatted(body, dtype):

names = steps_for(dtype)

return all(re.search(r"^## %d\. %s[ \t]*$" % (i, re.escape(n)), body, re.M)

for i, n in enumerate(names, start=1))

def read_meta(body, spec):

"""Pull already-filled values out of the 개요 table so a rewrite keeps them."""

m = re.search(r"^##[ \t]+%s[ \t]*$" % re.escape(spec["steps"][0]), body, re.M)

if not m:

return {}

chunk = body[m.end():]

nxt = re.search(r"^##[ \t]", chunk, re.M)

if nxt:

chunk = chunk[:nxt.start()]

out = {}

for row in re.findall(r"^\|(.+?)\|(.*?)\|[ \t]*$", chunk, re.M):

label = row[0].strip().strip("*").strip()

val = row[1].strip()

if label in ("항목", "") or set(label) <= set("-: "):

continue

if val and val != DASH:

out[label] = val

return out

DIRT = {"01_Tasks": "작업", "02_TechDocs": "기술문서", "03_TechDocs": "기술문서",

"03_Meetings": "회의록", "04_References": "참고자료"}

def rebuild_meta(body, spec, fm):

"""Replace the 개요 table in an already-formatted note with the current style."""

existing = read_meta(body, spec)

m = re.search(r"^##[ \t]+%s[ \t]*$" % re.escape(spec["steps"][0]), body, re.M)

if not m:

return body

start = m.end()

chunk = body[start:]

nxt = re.search(r"^##[ \t]", chunk, re.M)

end = start + (nxt.start() if nxt else len(chunk))

section = body[start:end]

tbl = re.search(r"^\|.*\|[ \t]*\n\|[-: |]+\|[ \t]*\n(?:\|.*\|[ \t]*\n?)*", section, re.M)

new_tbl = meta_table(spec, fm, existing)

if tbl:

section = section[:tbl.start()] + new_tbl + "\n" + section[tbl.end():]

else:

section = "\n" + new_tbl + "\n" + section.lstrip("\n")

return body[:start] + section + body[end:]

changed, skipped = [], 0

def build(title, spec, fm, legacy):

parts = ["# %s" % title, ""]

for i, name in enumerate(spec["steps"], start=1):

parts += ["## %s" % name, ""]

if i == 1:

parts += [meta_table(spec, fm), ""]

if i == spec["landing"] and legacy.strip():

parts += [legacy.strip(), ""]

return "\n".join(parts).rstrip() + "\n"

changed, migrated = [], 0

for dirpath, dirnames, filenames in os.walk(ROOT):

dirnames[:] = [d for d in dirnames if d not in (".obsidian", "resources")]

parts = os.path.relpath(dirpath, ROOT).replace(os.sep, "/").split("/")

dtype = next((DIRT[p] for p in parts if p in DIRT), None)

if not dtype:

continue

spec = SPEC[dtype]

for fn in sorted(filenames):

if not fn.endswith(".md") or fn in ("index.md", "README.md"):

continue

fmblock, fm, body = split_fm(raw)

title = fm_get(fm, "title") or fn[:-3]

if already_formatted(body, dtype):

skipped += 1

continue

body2 = migrate_headings(body, spec)

if is_formatted(body2, spec):

new_body = rebuild_meta(body2, spec, fm).strip() + "\n"

migrated += 1

else:

legacy = shift_headings(strip_leading_h1(body.strip()).strip())

new_body = build(title, spec, fm, legacy)

legacy = shift_headings(strip_leading_h1(body.strip()).strip())

new_body = build(title, dtype, fm, legacy)

new = fmblock.rstrip("\n") + "\n\n" + new_body

if new != raw:

changed.append(os.path.relpath(full, ROOT).replace(os.sep, "/"))

if APPLY:

io.open(full, "w", encoding="utf-8", newline="\n").write(new)

print("본문 양식 적용: %d건 (이미 적용됨 %d건, apply=%s)" % (len(changed), skipped, APPLY))

print("본문 양식 적용: %d건 변경 (기존 양식 인식 %d건, apply=%s)" % (len(changed), migrated, APPLY))

if "--show" in sys.argv:

for c in changed:

print(" ", c)

**Error:** You've hit your monthly spend limit · raise it at claude.ai/settings/usage?from=cc_cli_limit_message · your session limit resets 7:20pm (Asia/Seoul)

* Cogitated for 1m 54s
