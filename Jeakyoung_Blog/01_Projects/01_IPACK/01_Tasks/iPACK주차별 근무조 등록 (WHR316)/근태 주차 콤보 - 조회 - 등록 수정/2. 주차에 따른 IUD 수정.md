---
title: 2. 주차에 따른 IUD 수정
date: 2026-06-10
type: 작업
project: IPACK
status: 진행중
priority: 보통
assignee: []
tags: []
---

# 2. 주차에 따른 IUD 수정

## 📋 개요

| 항목 | 내용 |
|:--|:--|
| **요청자** | — |
| **요청일** | 2026-06-10 |
| **대상 시스템** | iPlusERP |
| **관련 화면·프로그램** | [[01_Projects/01_IPACK/02_TechDocs/근태_2기능스펙/[기술] WHR316 주차별 근무조 등록\|WHR316 주차별 근무조 등록]] |

## 🔍 현상 및 원인

기존 `SP_WHR316_01_IUD`가 `WG_FROM_DATE`를 받지 않아, 한 사원이 여러 주차의 근무조를 따로 가질 수 없었음(주차 구분 없이 계속 덮어씀).

## 🔧 조치 내용

---

### 코드 내용

```sql
USE [iPlusERP_IPACK_DEV]
GO
/****** Object:  StoredProcedure [dbo].[SP_WHR316_01_IUD]    Script Date: 2025-09-11 오전 8:58:38 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO


ALTER PROC	[dbo].[SP_WHR316_01_IUD]
(
		@FACTORY_CODE	VARCHAR(6)
	,	@EMPLOYEE_NO	VARCHAR(30)
	,	@WG_CODE		VARCHAR(6)
	,	@OPMAN_CODE		VARCHAR(10)
	, @WG_FROM_DATE	VARCHAR(8)
)
AS
BEGIN
SET NOCOUNT ON;

	UPDATE TIN312W 
	SET 
			WG_CODE			=	@WG_CODE
		,	OPMAN_CODE		=	@OPMAN_CODE
		,	OPTIME			=	CONVERT(VARCHAR(8), GETDATE(), 112) + LEFT(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''), 4)
		,	WG_FROM_DATE	=	@WG_FROM_DATE
		,	WG_TO_DATE		=	CONVERT(VARCHAR(8), DATEADD(DAY, 6, CONVERT(DATE, @WG_FROM_DATE)), 112)
	WHERE	FACTORY_CODE	=	@FACTORY_CODE
	AND		EMPLOYEE_NO		=	@EMPLOYEE_NO	
	IF	@@ROWCOUNT = 0 
		BEGIN
			INSERT INTO TIN312W
			( 
				  FACTORY_CODE 
				, EMPLOYEE_NO 
				, WG_CODE
				, WG_BIGO
				, OPMAN_CODE
				, OPTIME
				, WG_FROM_DATE
				, WG_TO_DATE
			) 
			VALUES 
			( 
				  @FACTORY_CODE
				, @EMPLOYEE_NO
				, @WG_CODE
				, ''
				, @OPMAN_CODE
				, CONVERT(VARCHAR(8), GETDATE(), 112) + LEFT(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''), 4)
				, @WG_FROM_DATE
				, CONVERT(VARCHAR(8), DATEADD(DAY, 6, CONVERT(DATE, @WG_FROM_DATE)), 112)
			) 
		END
END

```

Controller 내부에 Value 를 FROM_DATE만 쓰고 있어 WG_TO_DATE에 대해 시작일 형변환 후

6일 뒤를 더해 UPDATE하는 방식을 이용했습니다.

```java
while (iterator.hasNext()) {

					JSONObject jsonobj = (JSONObject) iterator.next();

					query = " EXEC SP_WHR316_01_IUD " 

							+ "\r\n" + "'"  + (String)jsonobj.get("FACTORY_CODE") + "'"
							+ "\r\n" + ",'" + (String)jsonobj.get("EMPLOYEE_NO")  + "'"
							+ "\r\n" + ",'" + (String)jsonobj.get("WG_CODE") 	  + "'"
							+ "\r\n" + ",'" + (String)jsonobj.get("OPMAN_CODE")   + "'"
							+ "\r\n" + ",'" + (String)jsonobj.get("WG_FROM_DATE")   + "'";
							
					System.out.println(query);
					
					stmt = conn.prepareStatement(query);
					stmt.executeUpdate();
				}
			}
```

앞단에서 받아온 정보를 쿼리에 실어주었습니다.

```java
if (rec.dirty) {
                rec.set('FACTORY_CODE', me.getView().lookup('combo_01').getValue());
                rec.set('OPMAN_CODE', gUserId);
                rec.set('WG_FROM_DATE', me.getView().lookup('combo_05').getValue());
            }
```

이후 onReg 동작시 필요한 combo_05 ( 월에 따른 주차 정보)를 담아주었습니다.

---

### 문제점

정상적으로 FROM, TO에 combo에서 받아온 주차정보를 등록할수 있지만 저장할때 한사람이 주차별로

여러 값을 가지고 있는것이 아닌 구분되어 있는 한사람의 기존 정보를 지우고 업데이트 하는 방식을

이용중이라 한사람이 하나의 주차 정보만 가지고 있을수 있습니다. **오류 수정때 추가 수정.**

## ✅ 검증 및 결과

- [x] 현재 `src/com/HR/WHR300/jvWHR316_IUD.java`(79~85행)의 `EXEC SP_WHR316_01_IUD` 호출이 이 문서가 추가한 `WG_FROM_DATE` 파라미터까지 포함한 5개 파라미터 형태로 남아있음 — 이 수정이 최종적으로 반영됐다.
- [x] 이 문서 말미에 남긴 "한 사람이 하나의 주차 정보만 가질 수 있는 문제"는 스스로 인지한 미해결 이슈였고, 이후 [[01_Projects/01_IPACK/01_Tasks/iPACK주차별 근무조 등록 (WHR316)/FIX) 버그 수정 - 기능개선|FIX) 버그 수정 - 기능개선]]에서 "주차 시작일이 같은 경우에만 UPDATE, 나머지는 INSERT"로 해결됨.

## 🔗 참고

- [[01_Projects/01_IPACK/02_TechDocs/근태_2기능스펙/[기술] WHR316 주차별 근무조 등록|WHR316 주차별 근무조 등록 스펙]]
- [[01_Projects/01_IPACK/01_Tasks/iPACK주차별 근무조 등록 (WHR316)/FIX) 버그 수정 - 기능개선|FIX) 버그 수정 - 기능개선]] — 여기서 언급한 문제의 후속 해결
