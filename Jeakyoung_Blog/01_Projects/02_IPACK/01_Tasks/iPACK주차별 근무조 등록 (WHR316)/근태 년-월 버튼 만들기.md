---
title: 근태 년-월 버튼 만들기
date: 2026-06-10
type: 작업
project: IPACK
status: 진행중
priority: 보통
assignee: []
tags: []
---

# 근태 년-월 버튼 만들기

## 📋 개요

| 항목 | 내용 |
|:--|:--|
| **요청자** | — |
| **요청일** | 2026-06-10 |
| **대상 시스템** | iPlusERP |
| **관련 화면·프로그램** | [[01_Projects/02_IPACK/02_TechDocs/근태_2기능스펙/[기술] WHR316 주차별 근무조 등록\|WHR316 주차별 근무조 등록]] |

## 🔍 현상 및 원인

사용자가 조회할 년/월을 바꿀 방법이 없어, 화살표 버튼으로 이전/다음 달 이동 기능이 필요했음.

## 🔧 조치 내용

---

### 요청사항

1. 사용자가 년/월을 변경할 수 있는 화살표 버튼을 추가합니다. 좌측화살표
(이전 달), 우측화살표(다음 달)

---

### 스크린 작업

```javascript
{
                                            xtype: 'displayfield',
                                            margin: '0 5 0 30',
                                            height: 20,
                                            value: '근태년월',
                                        },
                                        {
                                            xtype: 'button',
                                            text: '&lt;',
                                            itemId: 'WHR316_VW_Previous',
                                            margin: '0 0 0 0',
                                            ui: 'default-small',
                                            scope: me,
                                        },
                                        {
                                            xtype: 'textfield',
                                            name: 'searchField',
                                            value: yyyy + '-' + mm,
                                            itemId: 'WHR316_VW_yymm',
                                            fieldStyle: 'text-align:center',
                                            hideLabel: true,
                                            margin: '0 0 0 0',
                                            width: 75,
                                        },
                                        {
                                            xtype: 'button',
                                            text: '&gt;',
                                            itemId: 'WHR316_VW_Next',
                                            margin: '0 0 0 0',
                                            ui: 'default-small',
                                            scope: me,
                                        },
```

View에서 displayfield를 선언 → 이전, 이후버튼 추가후 → yyyy-mm 형식으로 center field에 추가

```javascript
onView: function () {
        var me = this;

        var combo = me.getView().lookup('combo_01');
        var grid = me.getView().lookup('grid');
        grid.store.sorters.clear();
        var store = this.getViewModel().getStore('store_01');

        store.load({
            params: {
                FACTORY_CODE: me.getView().lookup('combo_01').getValue() ? me.getView().lookup('combo_01').getValue() : '',
                WG_CODE: me.getView().lookup('combo_04').getValue() ? me.getView().lookup('combo_04').getValue() : '',
                JI_GROUP_CODE: me.getView().lookup('combo_02').getValue() ? me.getView().lookup('combo_02').getValue() : '',
                PAY_TYPE_CODE: me.getView().lookup('combo_03').getValue() ? me.getView().lookup('combo_03').getValue() : '',
                DEPT_CODE: me.getView().lookup('columns_tf01').getValue() ? me.getView().lookup('columns_tf01').getValue() : '',
                BASE_NAME: me.getView().lookup('columns_tf02').getValue() ? me.getView().lookup('columns_tf02').getValue() : '',
                YYYYMM: Ext.util.Format.date(me.getView().down('#WHR316_VW_yymm').getValue(), 'Ym'),
            },
            callback: function (records, op, success) {
                if (success) {
                    grid.getView().refresh();
                }
            },
        });
    },
```

CONTROLLER 내부에 ONVIEW 검색 파라미터에 YYYYMM 추가

```javascript
uLMonthPrevious: function () {
        var me = this;
        var strDate;
        //        var nYymm = Ext.getCmp("IMTW901_VM_yymm").getValue();

        var nYymm = me.getView().down('#WHR316_VW_yymm').getValue();

        strDate = nYymm.substr(0, 4) + '-' + nYymm.substr(5, 2) + '-' + '01';
        var nDate = new Date(strDate);
        nDate = Ext.Date.add(nDate, Ext.Date.MONTH, -1);

        var mm = (nDate.getMonth() + 1).toString(); //January is 0!
        var yyyy = nDate.getFullYear().toString();
        mm = mm[1] ? mm : '0' + mm[0];
        //        Ext.getCmp("IMTW901_VM_yymm").setValue(yyyy + '-' + mm);
        me.getView()
            .down('#WHR316_VW_yymm')
            .setValue(yyyy + '-' + mm);

        this.onView();
    },
```

previous 버튼 동작 CT → Month를 가져와서 → -1

```javascript
uLMonthNext: function () {
        var me = this;
        var strDate;
        //        var nYymm = Ext.getCmp("IMTW901_VM_yymm").getValue();
        var nYymm = me.getView().down('#WHR316_VW_yymm').getValue();
        strDate = nYymm.substr(0, 4) + '-' + nYymm.substr(5, 2) + '-' + '01';
        var nDate = new Date(strDate);
        nDate = Ext.Date.add(nDate, Ext.Date.MONTH, 1);

        var mm = (nDate.getMonth() + 1).toString(); //January is 0!
        var yyyy = nDate.getFullYear().toString();
        mm = mm[1] ? mm : '0' + mm[0];
        //        Ext.getCmp("IMTW901_VM_yymm").setValue(yyyy + '-' + mm);
        me.getView()
            .down('#WHR316_VW_yymm')
            .setValue(yyyy + '-' + mm);

        this.onView();
    },
```

Next 버튼 동작 CT → Month를 가져와서 → +1

```sql
USE [iPlusERP_IPACK_DEV]
GO
/****** Object:  StoredProcedure [dbo].[SP_WHR316_01_LIST]    Script Date: 2025-09-10 오전 9:59:00 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

ALTER PROC	[dbo].[SP_WHR316_01_LIST]
(
		@FACTORY_CODE		VARCHAR(6)
	 ,	@PAY_GROUP_CODE		VARCHAR(10)
	 ,	@PAY_TYPE_CODE		VARCHAR(10)
	 ,	@WG_CODE			VARCHAR(6)
	 ,  @DEPT_NAME			VARCHAR(10)
	 ,	@BASE_NAME			VARCHAR(20) = ''
	 ,  @YYYYMM				VARCHAR(14) --쿼리내 YYYYMM추가 (FRONT 월 버튼사이 TEXT에서 넘어온정보)
)
AS
BEGIN

-- EXEC SP_WHR316_01_LIST '000001','','','',''
	SELECT  
		T1.EMPLOYEE_NO
		, T1.BASE_NAME 
		, T2.WG_CODE
		, T3.WG_NAME 
		, T1.DEPARTMENT_NAME 
		, T1.LEVEL_NAME 
		, T1.BASE_NAME
	FROM (
			SELECT	A.EMPLOYEE_NO
				,	A.BASE_NAME
				,	A.DEPARTMENT_CODE
				,	B.DEPARTMENT_NAME
				,	ISNULL(C.CODE_NAME_FULL,'') LEVEL_NAME
				,	B.VIEW_SEQ AS DEPARTMENT_SEQ 
				,	C.VIEW_SEQ 
			FROM TIN114 AS A
				LEFT JOIN TIN122 AS B
				ON A.DEPARTMENT_CODE = B.Department_Code 
				LEFT JOIN TCO101 C
				ON A.LEVEL_CODE = C.CODE_ID2 
				AND C.CODE_ID1 = '207'
			WHERE A.FACTORY_CODE = @FACTORY_CODE
			AND A.EMPLOYEE_NO <> '0000'
			AND ISNULL(A.REAL_USER_GBN, '0') = '1'
			AND (A.RETIRE_DATE='' OR A.RETIRE_DATE  IS NULL)
			AND	(@PAY_GROUP_CODE = '' OR (@PAY_GROUP_CODE <> '' AND A.PAY_GROUP_CODE = @PAY_GROUP_CODE))
			AND	(@PAY_TYPE_CODE = '' OR (@PAY_TYPE_CODE <> '' AND A.PAY_TYPE_CODE = @PAY_TYPE_CODE))
		) T1
		LEFT OUTER JOIN (
			SELECT FACTORY_CODE, EMPLOYEE_NO, WG_CODE, WG_FROM_DATE  
			FROM  TIN312W
			WHERE LEFT(WG_FROM_DATE, 6) = @YYYYMM  -- 조인 조건 추가/등록된 날짜를 년 월까지만 받아
		) T2 ON (T1.EMPLOYEE_NO = T2.EMPLOYEE_NO) -- 받아온 년 월과 같은것들만 SELECT하기
		LEFT OUTER JOIN (
			SELECT FACTORY_CODE , WG_CODE, WG_NAME FROM TIN310
		) AS T3 ON (T2.FACTORY_CODE = T3.FACTORY_CODE   AND T2.WG_CODE = T3.WG_CODE  )
	WHERE 1=1
	AND	(@WG_CODE = '' OR (@WG_CODE <> '' AND T2.WG_CODE = @WG_CODE))
	AND	(@DEPT_NAME = '' OR (@DEPT_NAME <> '' AND T1.Department_Name  = @DEPT_NAME))
	AND	(@BASE_NAME = '' OR (@BASE_NAME <> '' AND T1.BASE_NAME LIKE @BASE_NAME))
	ORDER BY T1.DEPARTMENT_SEQ, T1.VIEW_SEQ, T1.BASE_NAME 

END

```

---

### 문제점

조회 필터링 기능 자체는 정상적으로 동작하지만 IUD 등록과정에 문제가 있습니다.

```sql
USE [iPlusERP_IPACK_DEV]
GO
/****** Object:  StoredProcedure [dbo].[SP_WHR316_01_IUD]    Script Date: 2025-09-10 오전 9:30:14 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO


ALTER PROC	[dbo].[SP_WHR316_01_IUD]
(
		@FACTORY_CODE	VARCHAR(6)
	,	@EMPLOYEE_NO	VARCHAR(30)
	,	@WG_CODE		VARCHAR(6)
	,	@OPMAN_CODE		VARCHAR(10)


)

AS
BEGIN

	UPDATE TIN312W 
	SET 
			WG_CODE			=	@WG_CODE
		,	OPMAN_CODE		=	@OPMAN_CODE
		,	OPTIME			=	CONVERT(VARCHAR(8), GETDATE(), 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),4)
		,	WG_FROM_DATE	=	CONVERT(VARCHAR(8), GETDATE(), 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),0)
		,	WG_TO_DATE		=	CONVERT(VARCHAR(8), GETDATE() + 7, 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),0)
	WHERE	FACTORY_CODE	=	@FACTORY_CODE
	AND		EMPLOYEE_NO		=	@EMPLOYEE_NO
	
	IF	@@ROWCOUNT = 0 
		BEGIN

			INSERT INTO TIN312W
			( 
				FACTORY_CODE 
				, EMPLOYEE_NO 
				, WG_CODE
				, WG_BIGO
				, OPMAN_CODE
				, OPTIME
				, WG_FROM_DATE
				, WG_TO_DATE
			) 
			VALUES 
			( 
				@FACTORY_CODE
				,	@EMPLOYEE_NO
				,	@WG_CODE
				,	''
				,	@OPMAN_CODE
				,	CONVERT(VARCHAR(8), GETDATE(), 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),4)
				,	CONVERT(VARCHAR(8), GETDATE(), 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),0)
				,	CONVERT(VARCHAR(8), GETDATE() + 7, 112) + left(REPLACE(CONVERT(VARCHAR(8), GETDATE(), 108), ':', ''),0)
			) 
		END


END


```

해당 방법처럼 주차 정보에 대한 COMBO 구성과 주차 정보 등록 스토어나 콤보도 아직 만들지 않아

해당 절처럼 임의 추가 하는 방식 때문에 테이블 내부에 9월에 해당되는 데이터밖에 없습니다.

위 사항에 대한 수정안은 주차별 근무조 기능을 구현하면서 병행하겠습니다.

---

### 결과화면

![[image 140.png]]

월별 주차조회 조회입니다. 좌우측 버튼을 통해 월을 변경할수 있습니다.

![[image 141.png]]

해당 화면처럼 월에 따라 주차조건 콤보가 바뀌게 되어있습니다.

---

## ✅ 검증 및 결과

- [x] `WHR316_VW.js`에 `WHR316_VW_yymm`/`WHR316_VW_Previous`/`WHR316_VW_Next` itemId가 실제 존재(`WHR316_CT.js`의 `init()`에서 이 itemId들의 클릭 이벤트를 바인딩).
- [x] `uLMonthPrevious`/`uLMonthNext`가 `WHR316_CT.js`(316~356행)에 존재하나, 이 문서의 코드와 달리 최종 버전은 끝에서 `this.onView()`를 직접 부르지 않고 `me.onSetCombo_05(yyyy + mm)`만 호출 — 주차 콤보가 다시 로드되면서 `onComboChange`의 `combo_05` 케이스가 `onView()`를 대신 트리거하는 구조로 바뀜([[01_Projects/02_IPACK/01_Tasks/iPACK주차별 근무조 등록 (WHR316)/주차 미 선택시 오류사항 수정|주차 미 선택시 오류사항 수정]] 참고).
- [x] SP 파라미터도 이 문서 시점(`YYYYMM`)에서 이후 `WG_FROM_DATE` 단독으로 바뀜 — [[01_Projects/02_IPACK/01_Tasks/iPACK주차별 근무조 등록 (WHR316)/근태 주차 콤보 - 조회 - 등록 수정/3. 주차에 따른 조회 조건 변경|3. 주차에 따른 조회 조건 변경]] 참고.

## 🔗 참고

- [[01_Projects/02_IPACK/02_TechDocs/근태_2기능스펙/[기술] WHR316 주차별 근무조 등록|WHR316 주차별 근무조 등록 스펙]]
- [[01_Projects/02_IPACK/01_Tasks/iPACK주차별 근무조 등록 (WHR316)/근태 주차 콤보 - 조회 - 등록 수정/근태 주차 콤보 - 조회 - 등록 수정|근태 주차 콤보 - 조회 - 등록 수정]]
