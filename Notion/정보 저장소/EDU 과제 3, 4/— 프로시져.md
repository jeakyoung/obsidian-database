---

---
![[image 57.png]]

```sql
CREATE SEQUENCE SEQ_EMP_NO
    START WITH 1001
    INCREMENT BY 1;
```

→ TEST_SEQ 를 정의하여 속성을 정의 1001부터 시작하여 Next value 시 1씩 증가


```sql
VALUES (
            @FACTORY_CODE,
            NEXT VALUE FOR TEST_SEQ,
            @BASE_NAME,
            @JOIN_DATE,
            @DEPARTMENT_CODE,
            @OPMAN_CODE
        );
```

→ Value 에서 TEST_SEQ를 NEXT VALUE로 호출하여 1씩 증가 중복X
