---

---
---

## List Procedure

```sql
USE [iPlusERP_New_20250320]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER PROCEDURE [dbo].[SP_HHR101_LIST]

    --@FACTORY_CODE   VARCHAR(6),
    --@WORK_CODE      VARCHAR(6),
    --@WORK_NAME      VARCHAR(8),
    --@BASE_NAME      VARCHAR(5),
    --@OPTIME         VARCHAR(20),
    --@PROD_LIMIT_QTY VARCHAR(20),
    --@LINE_COUNT     VARCHAR(10),
    --@OUTORDER_FLAG  VARCHAR(2),
    --@VIEW_SEQ       VARCHAR(10),
    --@DELETE_FLAG    VARCHAR(1)    불필요한 부분 삭제 (SELECT 동작에는 불필요함)
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @TEMP VARCHAR(3) = '999';

    SELECT
          A.FACTORY_CODE
        , A.WORK_CODE
        , A.WORK_NAME
        , B.BASE_NAME
        , A.OPTIME
        , A.PROD_LIMIT_QTY
        , A.LINE_COUNT
        , A.OUTORDER_FLAG
        , C.CODE_NAME_ABBR
        , A.VIEW_SEQ
        , A.DELETE_FLAG
    FROM HHR101 AS A
    LEFT JOIN TIN114 AS B
        ON A.OPMAN_CODE = B.EMPLOYEE_NO
    LEFT JOIN TCO101 AS C
        ON C.CODE_ID1 = @TEMP
       AND C.CODE_ID2 = A.OUTORDER_FLAG
    WHERE ISNULL(A.DELETE_FLAG, '0') <> '1'
    ORDER BY TRY_CAST(A.VIEW_SEQ AS INT) ASC;
END

```

기존 프로시저내에 조회에 필요한 SELECT부분만 남겨두고 불필요한 부분 삭제.

---

## List Servlet

```java
package com.edu;

import java.io.IOException;
import java.io.PrintWriter;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.ResultSetMetaData;
import java.sql.Statement;
import java.util.ArrayList;
import java.util.List;

import javax.servlet.GenericServlet;
import javax.servlet.ServletContext;
import javax.servlet.ServletException;
import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;
import javax.servlet.annotation.WebServlet;

import org.json.simple.JSONArray;
import org.json.simple.JSONObject;

//import com.VO.APIBean;

@WebServlet("/com/edu/jvEdu5_01_LIST")
public class jvEdu5_01_LIST extends GenericServlet {
	private static final long serialVersionUID = 1L;

	@Override
	public void service(ServletRequest request, ServletResponse response)
			throws ServletException, IOException {
			Connection conn = null;
			Statement stmt = null;
			ResultSet rs = null;

			JSONArray itemList = new JSONArray();
			
		try {	
			
			ServletContext sc = this.getServletContext();
			
			Class.forName(sc.getInitParameter("driver")).newInstance();
			conn = DriverManager.getConnection(
					sc.getInitParameter("url"), //JDBC URL
					sc.getInitParameter("username"),	// DBMS 사용자 아이디
					sc.getInitParameter("password"));	// DBMS 사용자 암호
			stmt = conn.createStatement();
			
			response.setContentType("text/html; charset=UTF-8");
			
			request.setCharacterEncoding("UTF-8");
			
			String sql = "EXEC SP_HHR101_LIST " ; //프로시저 실행 쿼리를 담아주고
			
			rs = stmt.executeQuery(sql); //쿼리를 실행해서 반환값을 rs로 담아준뒤
			
			ResultSetMetaData rm = rs.getMetaData();
			
			while(rs.next())
			{
				JSONObject tempJson = new JSONObject();
				for(int nCount=1 ; nCount <=  rm.getColumnCount() ; nCount++)
				{	
					tempJson.put(rm.getColumnName(nCount), rs.getString(rm.getColumnName(nCount)));
				}
								
				itemList.add(tempJson); //JSON형태로 만들어 itemList으로 담기
			}
		} catch (Exception e) {
			throw new ServletException(e);
			
		} finally {
			try {if (rs != null) rs.close();} catch(Exception e) {}
			try {if (stmt != null) stmt.close();} catch(Exception e) {}
			try {if (conn != null) conn.close();} catch(Exception e) {}
		}
		PrintWriter out = response.getWriter();
		out.print(itemList); //이걸 그대로 출력
		out.flush();

	}
}

```

List 서블릿 부분에서 기존방식인 서블릿 → 모델설정 → 서비스호출 → 모델로 set → 서블릿에서 get

방식이 아닌 Statement를 사용하여 String sql = "EXEC SP_HHR101_LIST " ; 쿼리로 이미 SELECT가

정의되어있는 PROCEDURE를 호출한 뒤 반환값을 리스트에 담아 반환함으로서 로직을 훨씬 간단하고

직관적이게 수정했습니다.

---

## 실행 결과

![[image 40.png]]

해당 방법으로 호기 그리드 내 코드도 수정하였으며 정상적으로 조회가 가능하지만 기존의 데이터 필터가

이것도 수정했습니다. 

[[데이터 필터링 수정 코드]]

---
