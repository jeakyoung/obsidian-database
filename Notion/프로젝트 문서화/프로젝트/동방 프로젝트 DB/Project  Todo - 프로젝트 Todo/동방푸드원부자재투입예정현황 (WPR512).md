---
base: "[[Notion/프로젝트 문서화/프로젝트/동방 프로젝트 DB/Project  Todo - 프로젝트 Todo/Project  Todo - 프로젝트 Todo.base]]"
태그: []
상태: 완료
생성 일시: 2026-06-10T14:28:00
담당자: []
---
## ㅇ 요청사항

![[동방_430_요청사항.png]]

D.MATERIAL_CODE_ALTER + D.MATERIAL_CODE = NEW_MATARIAL_CODE

1 액상

2 분말

3 건조건더기

4 야채류

5 유지류

6 고형물

7 부재료

8 기타

9 항료/색소

PD, PE, PK

B박스

C캡

F필름

P파우치

L라벨

V비닐

Y용기

K기타

SP_WCO313_01_IUD → 운영에만 존재함

TCO403에 존재하는 MATERIAL_FLAG → 자재구분정보를 비교하여 부재료인 경우에만 부재료 분류 필터링

콤보박스 정보는 기준정보 - 자재관리 - 부재료 마스터 등록에 존재하는 jvWCO312_03_COMBO를 활용

SP_WCO312_03_COMBO 프로시저에 접근하는 방식 TCO_107에서 ITEM_KIND1 (대분류 자재구분 코드)를

CODE_ID를 016으로 CT에서 고정한뒤 콤보만 사용

해당콤보로 가져온 값을 @MID_ITEM_CODE 명칭에 해당되는 파라미터로 담아준뒤 검색조건으로 활용

조회조건은 TCO403테이블내에 존재하는 ITEM_KIND2값을 비교


작업부

BE : jvWPR512_01

FE : 

SQL : SP_WPR512_01_LIST


```javascript
else if (matGbn != '1120902') {
            me.getView().down('#cbo_midItem').setValue(null);
            me.getView().down('#cbo_midItem').setDisabled(true);
```

→ 해당방식처럼 value만이 아닌 disabled도 CT에서 내려줄수있음

```javascript
 listeners: {
               afterrender: function (cmp) {
               Ext.create('Ext.tip.ToolTip', {
                  target: cmp.getEl(),
                  html: '부재료 선택시 구분이 활성화됩니다.',
                });
              },
            },
```

→ 툴팁붙이기 → disabled에선 동작하지 않음 readOnly로


```javascript
onSetMidCombo: function () {
        var me = this;

        var store = this.getViewModel().getStore('cboStore_MidGbn');
        var matGbn = this.getView().down('#cbo_MatGbn').getValue();

        if (matGbn == '1120902') {
            me.getView().down('#cbo_midItem').setReadOnly(false);
            store.load({
                params: {
                    ITEM_KIND1: '016',
                },
                callback: function (records, op, success) {
                    if (success) {
                    }
                },
            });
        } else if (matGbn != '1120902') {
            me.getView().down('#cbo_midItem').setValue(null);
            me.getView().down('#cbo_midItem').setReadOnly(true);
        }
    },
```

→ 뷰에 ReadOnly 내리는법

## 결과화면

![[동방_430_결과화면_1.png]]

![[동방_430_결과화면_2.png]]

![[동방_430_결과화면_3.png]]
