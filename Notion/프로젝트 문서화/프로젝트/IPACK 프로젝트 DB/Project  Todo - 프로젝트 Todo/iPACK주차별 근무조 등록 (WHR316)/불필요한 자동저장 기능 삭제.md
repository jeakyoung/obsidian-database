---

---

---

## 요청사항

근무조 변환, 일괄적용 클릭시 저장버튼을 누르지 않아도 자동으로 store동작을 진행하고 있었음.

변환시 그리드뷰 내에서 볼수만 있고 저장을 클릭하지않으면 store.sync 동작하지 않기

---

```javascript
WHR316_VW_btnReg: function (btn) {
        var me = this;

        var grid = me.getView().lookup('grid');
        var store = grid.getStore();
        var selected = me.getView().lookup('combo_06').getValue();
        var RegCheck = false;

        store.each(function (record) {
            if (record.get('SELECT_FLAG') === true) {
                RegCheck = true;
            }
        });

        if (!RegCheck) {
            showToast('알림', '적용할 대상을 선택해주세요.');
            return;
        }

        store.each(function (rec) {
            if (rec.get('SELECT_FLAG') === true) {
                rec.set('FACTORY_CODE', me.getView().lookup('combo_01').getValue());
                rec.set('WG_CODE', selected);
                rec.set('OPMAN_CODE', gUserId);
                rec.set('WG_FROM_DATE', me.getView().lookup('combo_05').getValue());
            }
        });

        // store.sync({
        //     success: function () {
        //         showToast('알림', '일괄 적용을 완료하였습니다.');
        //         me.onView();
        //     },
        // });
    },

    WHR316_VW_btnCng: function (btn) {
        var me = this;
        var grid = me.getView().lookup('grid');
        var store = grid.getStore();

        store.each(function (rec) {
            var wgCode = rec.get('WG_CODE');
            if (rec.get('WG_CODE')) {
                rec.set('FACTORY_CODE', me.getView().lookup('combo_01').getValue());
                rec.set('OPMAN_CODE', gUserId);
                rec.set('WG_FROM_DATE', me.getView().lookup('combo_05').getValue());
            }
            if (wgCode === '001') {
                rec.set('WG_CODE', '002');
            } else if (wgCode === '002') {
                rec.set('WG_CODE', '001');
            }
        });

        // store.sync({
        //     success: function () {
        //         showToast('알림', '근무조를 변환하였습니다.');
        //         me.onView();
        //     },
        // });
    },
```

다음과 같이 원하는 필터링 동작만 진행하고 sync동작은 진행하지 않아 사용자가 저장버튼을 누르지

않는 경우 버튼하나로 자동저장 되지 않습니다.

---

## 결과화면

![[image 151.png]]

근무조 변환동작시 자동 저장이 아닌 grid내에 변경사항 적재, 대기

![[image 152.png]]

마찬가지 일괄적용도 grid내에 변경사항 적재, 대기

![[image 153.png]]

저장 클릭시에만 변경내용 반영

---