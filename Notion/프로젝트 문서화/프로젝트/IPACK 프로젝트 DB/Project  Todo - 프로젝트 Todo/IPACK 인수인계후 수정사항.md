---
base: "[[Notion/프로젝트 문서화/프로젝트/IPACK 프로젝트 DB/Project  Todo - 프로젝트 Todo/Project  Todo - 프로젝트 Todo.base]]"
태그: []
상태: 완료
생성 일시: 2026-06-10T14:29:00
시작일: 2025-10-21
담당자:
  - 안재경
---
## 1. 근무조별 일근태등록 (WHR314_VW)

1. ~~뒷쪽에 따라붙는 근무시간, 외출시간, 근태정보에 대한 내용이 수정되었을때 선택사항 체크될수 있도록~~ 

2. ~~계산식을 거친뒤 나오는 값이 0.20과같이 우측상단에있는 분단위 표와 맞지 않음 계산식에 들어가기전~~

~~분단위 시간의 정수 1의 자릿수에서 반올림하는 과정을 통해 값을 10분단위로 일관화 하는 과정이 필요~~

3. ~~근무시간 항목에 대해 근태기에 따라 자동세팅되는 구조를 사용자가 임의로 수정할수 있도록 수정~~

~~( 현재방식은 프로시저단에서 FIRST_IN ~ LAST_OUT를 BASE_DIS로 가져오고 있음  )~~

4. ~~근무시간에 대한 시간 수정이 발생했을대 뒷쪽에 따라붙는 근태정보 수정필요~~

~~( 3번과정을 해결하여 최초입실, 최종퇴실을 임의로 저장한다면 문제되지 않음 기존방식도 입실 퇴실시간을 CONTRILLER에 임의로 가져온뒤 계산하고 저장하는 과정을 거치고있어 제대로 연결만 해주면됨)~~

5. 근무조별일근태등록 (WHR314_VW)
    1. ~~기본근무적용이 되면 근무조에 설정된 기본근무시간이 들어갈 수 있도록 함, 주간조면 09:00~18:00
그리고 기록컬럼에 O가 되어야함~~

    2. ~~외출시간이나 근태정보에 나와있는 시간계산에 대해서 정확하게 표현이되어야함~~

    3. ~~저장이 안된 상태에서도 사원의 근무조는 반드시 보여야함~~

    4. ~~근태종합집계에서 상단에는 근무횟수와 시간이 표현되어야함, 사원명컬럼 바로 뒤로 추가~~

## 2. 근무종합집계, 기간별 근태현황

6. ~~공통사항으로 특근시간을 추가해야함 ~~

~~EX) 주말 근무시 전체근무시간 : 특근시간~~

## 2025.10.23 추가작업

ㅇ 뒷쪽에 따라붙는 근무시간, 외출시간, 근태정보에 대한 내용이 수정되었을때 선택사항 체크될수 있도록 

ㅇ 야간조에 대한 계산식 재수정이 필요.

ㅇ 공통사항으로 특근시간을 추가해야함 

EX) 주말 근무시 전체근무시간 : 특근시간

- 근태일자 변경했을 때 조회되도록 변경
- 특근시간 - 일요일, 정규시간으로
- 특근수당 - 토요일, 정규시간으로
- 출근,퇴근시간 보이는건 정확하게
- 외출시간은 실제 분단위로 나오게 계산아님
- 특근시간 계산이 처음 insert할 때 되는게 아니고 수정할 때 됨
- 기간별 근태현황 너무 오래걸림


- 야간조는 하루를 빼야하는거고
- 주간별 근무조설정에서 근태주차는 오늘날짜가 포함된 날짜로 세팅이 되도록
- 기간별근태현황 저장되어있는걸 보여줘야되니까 저장이 안되어있을 땐 입출테이블 TIN334, 저장이 되어있으면 TIN313


## 2025-10-28

CAL값을 리스트프로시저에서 미리뜸 → IUD동작이 굴러갈때 CAL값을 가져와서 계산 → DSTIME 변경

근데여기서 

계산부만 따로빼서 사전 동작 이후 onReg를 통해 저장동작 진행 (속도개선? X → 계산부분, 저장부분을 분리해서 보다 즉각적인 피드백을 위해.)

저장할때 시간자체는 정상적으로 저장되지만 저장된 시간을 바탕으로 계산식을 적용할때 onReg 동작이 한번더 시행되어야지만 뒷쪽에 붙는 ds_time 정보가 수정됨.

** 이전값이 적용되는 이유 FIRST_IN_CAL쪽을 보면 조회로 쓰고있는 B.WG_PRDFROM2는 이전값임

IUD를 동작시키면 LIST프로시저에서 불러온 값으로 DS_TIME계산을 완료한뒤 다시 IUD에 집어넣는데

FIRST_IN_DIS를 저장하고자 할때 방금 수정된값을 가져올수가 없고 저장되어있던 DIS를 불러온 뒤에 

기존 데이터를 들고 계산 로직을 수행한뒤에 DS_TIME을 저장하는 과정에서 DIS값을 같이저장함 이때문에 저장로직을 이용 업데이트 하는 과정에서 새로입력받은 DIS값이 아닌 한 사이클씩 밀린 상태로 DS_TIME이 집계됨


계산식을 거쳐서 나온 ds_time 값이 vbm과 다름 기존 모듈에서 계산식을 여러번 테스트해서 지금 완성 되어있는 계산식 수정하기

TIN334 테이블에 대해 KT_DATE를 가져오는 TIN334_GET_KDDATE 부분의 동작이 정상적으로 이루어지지 않았음 → 때문에 기존 CTE로는 분별이 불가능한 데이터가 발생 해당부에 대한 조건 추가필요.



```javascript
var grid = this.getView().down('grid');

        grid.on('edit', function (editor, e) {
            var record = e.record;
            record.set('SELECT_FLAG', true);
        });

```

edit 확인해서 select_flag true로 만들어버리기!


건드린 파일 25.10.31

WHR 328 02

whr 320_01

WHR314는 싹다
