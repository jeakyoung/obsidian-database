
74.35 -> SONGYEON_DEV
WE -> WMA321 참조

구매자재 -> 입고등록화면을 앱으로 구현할예정

1 -> 입고 등록 화면, 거래처 / 품목 코드를 넣어서 이미지 등록하는 화면, 입고 대상 조회, 입고 등록해야 하는 화면 조회, 입고 현황 조회, 품목 선택, 구매 처 선택

2 -> 라벨 매칭 화면

프로세스
구매 요청 등록 -> 확정 -> 발주 -> 가입고 등록 ->  원자재 입고 검사 등록( 수입 검사 ) -> 입고 등록

- 구매요청 -> TMA103, 104 등록테이블
- 구매요청확정 -> TMA103A 업데이트 ( purchace_confirm ) key -> 요청순번
- 발주등록 -> TMA107, 108 등록 테이블
- 가입고등록 -> TMA203, 204 
- 수입검사 -> TQC205, TQC206 -> TCO403(제품마스터) 내 INSPECT_FLAG로 구분
- 입고등록 -> TMA205, 206
- 입고등록 라벨첨부 -> TMA206 ( 미확정 )


입고대상 조회조건 : 검사일, 구매처( 팝업 ), 품목명 ( 팝업 )

입고 등록 조건 : 
조회 파라미터( 품목코드, 품목명, 규격, 단위 ) 
저장 파라미터 (입고 수량, LOT, 이미지라벨링 )

입고저장시 -> 반드시 같은 구매처끼리 묶여서 저장되어야만함
