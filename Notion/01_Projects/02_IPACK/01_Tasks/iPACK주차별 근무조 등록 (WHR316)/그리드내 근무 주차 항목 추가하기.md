---

---
---

## 요청 사항

그리드내 근무 주차 항목에 대해 행을 추가하여 보여줄수 있도록 재수정하기

---

## 진행 사항

```sql
ALTER PROC	[dbo].[SP_WHR316_01_LIST]
(
		@FACTORY_CODE		VARCHAR(6)
	 ,	@PAY_GROUP_CODE		VARCHAR(10)
	 ,	@PAY_TYPE_CODE		VARCHAR(10)
	 ,	@WG_CODE			VARCHAR(6)
	 ,  @DEPT_NAME			VARCHAR(10)
	 ,	@BASE_NAME			VARCHAR(20) = ''
	 ,  @WG_FROM_DATE		VARCHAR(8)
)
AS
BEGIN

-- EXEC SP_WHR316_01_LIST '000001','','','',''
	SELECT  
		  T1.EMPLOYEE_NO
		, T1.BASE_NAME 
		, T2.WG_CODE
		, T3.WG_NAME 
		, T1.DEPARTMENT_NAME 
		, T1.LEVEL_NAME 
		, T1.BASE_NAME
		, T2.WG_FROM_DATE
		, T2.WG_TO_DATE
```

우선 프로시저에서 list호출시 시작, 끝일을 같이 select할수 있게 해줍니다.

```javascript
store_01: {
            fields: [
                { name: 'SELECT_FLAG', type: 'bool' },
                { name: 'WG_FROM_DATE', type: 'date', dateReadFormat: 'Ymd' },
                { name: 'WG_TO_DATE', type: 'date', dateReadFormat: 'Ymd' },
            ],
```

이후 store에서 date타입의 포맷을 지정해준 뒤

```javascript
 {
                    text: '근무주차 시작일',
                    xtype: 'datecolumn',
                    style: 'text-align:center;',
                    align: 'left',
                    width: gColdWidth_BaseNmHR,
                    dataIndex: 'WG_FROM_DATE',
                    format: 'Y-m-d',
                },
                {
                    text: '근무주차 마감일',
                    xtype: 'datecolumn',
                    style: 'text-align:center;',
                    align: 'left',
                    width: gColdWidth_BaseNmHR,
                    dataIndex: 'WG_TO_DATE',
                    format: 'Y-m-d',
                },
```

다음과 같이 년-월-일 형식으로 그리드내에 출력해줍니다.

```javascript
WHR316_VW_btnCng: function (btn) {
        var me = this;
        var grid = me.getView().lookup('grid');
        var store = grid.getStore();

        store.each(function (rec) {
            var wgCode = rec.get('WG_CODE');
            if (rec.get('WG_CODE')) {
                rec.set('FACTORY_CODE', me.getView().lookup('combo_01').getValue());
                rec.set('OPMAN_CODE', gUserId);
                rec.set('WG_FROM_DATE', me.getView().lookup('combo_05').getValue());
            }
            if (wgCode === '001') {
                rec.set('WG_CODE', '002');
            } else if (wgCode === '002') {
                rec.set('WG_CODE', '001');
            }
        });

        store.sync({
            success: function () {
                showToast('알림', '근무조를 변환하였습니다.');
                me.onView();
            },
        });
    },
```

해당 기능 구현시 발견하게된 오류 사항으로 변환 클릭시 적용할필요가 없는 행에도 근무주차 시작, 마감일을 등록하는것

을 확인했습니다.

이를 해결하기위해 if절을 추가하여 WG_CODE가 있는경우에만 업데이트를 진행하게 됩니다.

---

## 결과

![[image 147.png]]


---
